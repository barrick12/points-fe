import { render, screen } from '@testing-library/react';
import { Loading } from './Loading';

const testId = 'loading-test-id';

describe('[Loading]', () => {
  it('should render without issue', () => {
    render(<Loading testId={testId} />);
    screen.getByTestId(testId);
  });
});
